---
- name: Setup Linux work station
  hosts: shrine_azure

  vars_files:
    - ../../vault/user

  roles:
    # === SYSTÈME BASE ===
    - { role: system_upgrade }
    - { role: packages_generique }
    - { role: ssh }
    - { role: git }

    # === ZSH ===
    # - { role: nerd_font, when: "'microsoft' not in ansible_kernel" }
    - { role: zsh }
    
    # === OUTILS DE DÉVELOPPEMENT ===
    - { role: vim }

    # === UTILISATEUR ===
    - { role: admin }
    - { role: user }
